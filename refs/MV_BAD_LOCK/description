The variable allows systems with broken locking mechanism to still work
properly.
If the variable is set, it makes sure the <option>PIDFile</option> lock is
not destroyed when the lock file is read.
<userinput>interchange -stop</userinput> will then work properly.
